% !TEX TS-program = sepdflatexmk
% NOT THIS ONE !TEX root = ../main/main.tex NOT THIS ONE
% !TEX spelling = en_US
% !BIB engine = biblatex

\documentclass{article} % Comment

% Other Packages
\usepackage{siu%lo}
			nitx} % This is legal. Yeah.


% Layout Packages
\usepackage[a4paper]{geometry} % Page settings # showframe option for visible borders
	\geometry{
		top = 1in,
%		bottom = 1.25in,
		left = 1.25in,
		right = 1.25in,
%		showframe,
	}
\usepackage[
	draft,
]{graphicx} % Images
	\graphicspath{{../images/}} % Image directory

\usepackage{microtype} % Minor details

\usepackage{booktabs} % Table formatting

\usepackage{setspace} % Line spacing
	\onehalfspacing % 1 1/2 line spacing

\usepackage{parskip} % Unindented paragraphs

\usepackage{enumitem}

\usepackage{fancyhdr} % Headers and footers
	\pagestyle{fancy}
	\renewcommand{\headrulewidth}{0pt}
	\renewcommand{\footrulewidth}{0pt}
	\lhead{}
	\chead{}
	\rhead{\thepage}
	\lfoot{}
	\cfoot{}
	\rfoot{}


% Graphing
\usepackage{pgfplots}
	\pgfplotsset{
		compat = 1.14,
		table/search path={.,../data},
	}
	\usepgfplotslibrary{external}
	\tikzexternalize[prefix = ./tikz/]


% Format Commands
\frenchspacing

\usepackage{fancyvrb}
	\fvset{
		frame=single,
		numbers=left,
		numbersep=3pt,
		tabsize=4,
		showtabs=false,
	}


% Experimental Commands
\overfullrule = 2cm

\usepackage{hyperref} % Coloured for now \bm dwad
	\hypersetup{
		pdftitle = {PGFplots Guide},
		pdfauthor = {Benjamin Gray},
		pdfsubject = {pgfplots},
		pdfproducer = {LaTeX},
%		pdfcreator = {},
%		pdfkeywords = {},
		colorlinks=false,
%		linkcolor=blue,
%		filecolor=magenta,
%		urlcolor=cyan,
%		bookmarks = true,
		bookmarksopen = true,
	}
	\urlstyle{same} % make links in same font as document


\makeatletter
\newenvironment{desc}
  {\if@nobreak
     \vskip-\lastskip
     \vspace*{-2.5ex}
   \fi
   \decl}
  {\enddecl}
\makeatother


% Document Start
\begin{document}

\textbf{Hello}

\end{document}
