% !TEX TS-program = sepdflatexmk
% !TEX root = ../main/main.tex
% !TEX spelling = en_US
% !BIB engine = biblatex

\documentclass{article dwad} % Comment

% Other Packages
\usepackage[a4paper]{Hel%lo}
			llo} % This is legal. Yeah.


% Layout Packages
\usepackage[a4paper]{geometry} % Page settings # showframe option for visible borders
	\geometry{
		top = 1in,
%		bottom = 1.25in,
		left = 1.25in,
		right = 1.25in,
%		showframe,
	}
$ Hello world$
\usepackage[
	draft,
]{graphicx} % Images
	\graphicspath{{../images/}} % Image directory

\usepackage{microtype} % Minor details

\usepackage{booktabs} % Table formatting

\usepackage{setspace} % Line spacing
	\onehalfspacing % 1 1/2 line spacing

\usepackage{parskip} % Unindented paragraphs

\usepackage{enumitem}

\usepackage{fancyhdr} % Headers and footers
	\pagestyle{fancy}
	\renewcommand{\headrulewidth}{0pt}
	\renewcommand{\footrulewidth}{0pt}
	\lhead{}
	\chead{}
	\rhead{\thepage}
	\lfoot{}
	\cfoot{}
	\rfoot{}


% Graphing
\usepackage{pgfplots}
	\pgfplotsset{
		compat = 1.14,
		table/search path={.,../data},
	}
	\usepgfplotslibrary{external}
	\tikzexternalize[prefix = ./tikz/]


% Format Commands
\frenchspacing

\usepackage{fancyvrb}
	\fvset{
		frame=single,
		numbers=left,
		numbersep=3pt,
		tabsize=4,
		showtabs=false,
	}


% Experimental Commands
\overfullrule = 2cm

\usepackage{hyperref} % Coloured for now
	\hypersetup{
		pdftitle = {PGFplots Guide},
		pdfauthor = {Benjamin Gray},
		pdfsubject = {pgfplots},
		pdfproducer = {LaTeX},
%		pdfcreator = {},
%		pdfkeywords = {},
		colorlinks=false,
%		linkcolor=blue,
%		filecolor=magenta,
%		urlcolor=cyan,
%		bookmarks = true,
		bookmarksopen = true,
	}
	\urlstyle{same} % make links in same font as document


\makeatletter
\newenvironment{desc}
  {\if@nobreak
     \vskip-\lastskip
     \vspace*{-2.5ex}
   \fi
   \decl}
  {\enddecl}
\makeatother


% Document Start
\begin{document}

\input{../sections/titlepage}
	\clearpage
\tableofcontents
	\clearpage
\input{../sections/ideas}
	\newpage

\textbf{Hello}

\end{document}


% Just random stuff to find odd behaviour.

w dwad
\usepackage{Article,
	si={
		Hello={
			What? Nested Arguments?
		},
		world,
	}
}
wd
wda \[hello\] adw $dwad$
